# Known issues

Deliberately deferred findings, verified against `main` as of this branch. Everything here is
already known: a reviewer should **not** re-report it, and anything **not** listed is fair game.

How items get here:

- **Task reviews and final reviews** during implementation record minors that were judged not worth
  a fix round at the time (mostly test-coverage gaps and micro-inefficiencies).
- **ChatGPT PR reviews**: findings accepted as real but explicitly deferred, plus follow-ups spotted
  during a re-review after the fix wave landed.

Each row says where it lives, what it is, why it was left, and the obvious fix where there is one.
Items that were fixed since being recorded have been removed rather than kept as history.

Product-level gaps (features not built yet, as opposed to defects) live in
[superpowers/specs/v2-notes.md](superpowers/specs/v2-notes.md); see [Product-level gaps](#product-level-gaps) below.

## core

| Area | Issue | Why deferred | Suggested fix |
| --- | --- | --- | --- |
| [actors.ts](../src/core/src/actors.ts) | `resolveCredential` hashes the credential and queries `agents` for every credential, including Weave secrets | micro-cost, one extra query per call | order lookups by likelihood, or give credentials a type prefix |
| [actors.ts](../src/core/src/actors.ts) | Agent error asymmetry: a malformed `weaveId` is `weave_not_found`, an unknown-but-valid one is `forbidden` ("Join the Weave first") | both are safe answers; deliberate about not leaking existence | pick one code if clients ever switch on it |
| [agents.ts](../src/core/src/agents.ts) | `revokeAgent` is read-then-write (select, check, update), not atomic | no user-visible effect: concurrent revokes converge on revoked | `update … where revoked_at is null returning *` |
| [agents.ts](../src/core/src/agents.ts) | `listAgents` selects every column including `key_hash`, then strips it in `toPublicAgent` | the hash never leaves the process | select explicit columns |
| [db/schema.ts](../src/core/src/db/schema.ts) | Agent names are not unique instance-wide; two agents may share a name | design choice: agents are addressed by key, participants are unique per Weave | unique index on `lower(name)` if names become addressable |
| [db/schema.ts](../src/core/src/db/schema.ts) | No GIN index on `events.payload`: inbox (`->>'participantId'`, `? mentions`) and the invite idempotency predicate scan | scans are thread- or Weave-scoped and volumes are small | `jsonb_path_ops` GIN index when a Weave's log grows |
| [inbox.ts](../src/core/src/inbox.ts) | Row→event mapping duplicates `toEvent` in [events.ts](../src/core/src/events.ts) (module-private there) | copied verbatim from the brief; drift is test-visible | export `toEvent` and reuse it |
| [threads.ts](../src/core/src/threads.ts) | Thread URLs are not normalized: `…/pull/7` and `…/pull/7/` are distinct values | design choice — store what the human typed | normalize on write if dedupe ever matters |
| [weaves.ts](../src/core/src/weaves.ts) | `joinWeave`'s `alreadyJoined` early return skips the archived check: an agent that already joined gets its token back for an archived Weave | ruled read-shaped in the v2 final review (writes still fail) | check `archivedAt` before returning |
| [guidelines.ts](../src/core/src/guidelines.ts), [export.ts](../src/core/src/export.ts) | `getInstanceGuidelines` falls back to `getSettings`, which **inserts** the `settings` row when it is missing, and `getWeave` calls it — so `exportWeave`, whose transaction is `{ isolationLevel: "repeatable read", accessMode: "read only" }`, attempts a write inside a read-only transaction. Postgres rejects it with SQLSTATE `25006` and the **export fails** as `internal`; `onConflictDoNothing` does not help, because the statement never runs | unreachable in practice, not benign when reached: it needs a `settings` row that has never been created, and `createWeave` calls `getSettings` before it inserts anything, so no Weave can exist without that row. Every other entry point that could reach `getWeave` therefore runs after the row exists | return `DEFAULT_INSTANCE_GUIDELINES` without inserting when the row is missing, or hoist the settings read outside the export transaction |
| [index.ts](../src/core/src/index.ts) | `forThread` loads the Thread, then the delegate loads it again (`setThreadUrl`, `closeThread`, `postMessage`, `inviteParticipant`) | one extra primary-key read | pass the loaded row through |
| [export.ts](../src/core/src/export.ts) | `exportWeave` holds one pooled connection and a repeatable-read snapshot open for the whole paging loop, so a large log pins that connection (pool `max: 10`) and holds back the vacuum horizon | the snapshot is the point — metadata and events must describe one moment — and exports are rare and small today | page outside the snapshot against a recorded `lastSeq`, or stream the export |
| [test/](../src/core/test) | `resolveCredential` precedence (participant → keeper → agent → secret) untested | test coverage only | one table-driven test |
| [test/](../src/core/test) | [bus.ts](../src/core/src/bus.ts) bad-listener isolation (a throwing subscriber must not break `publish`) untested | test coverage only | subscribe a thrower plus a good listener |
| [test/db.test.ts](../src/core/test/db.test.ts) | v2 column queries lack a `table_schema = 'public'` filter; FK and unique indexes (`participants_weave_agent_idx`, `agents.key_hash`) not asserted | test coverage only | filter the schema, assert `pg_indexes` |
| [test/agents.test.ts](../src/core/test/agents.test.ts) | Guard tests assert only the error code for `assertCanRead`/`assertParticipantOf`/`assertIsKeeperOf`, not the "Join the Weave first" message | test coverage only (`actorId` and `assertStillKeeperOf` do assert it) | assert the message too |
| [test/export.test.ts](../src/core/test/export.test.ts) | Thread `<url>` line and the `thread.invited` / cleared-`thread.url_changed` system lines unasserted | test coverage only | extend the md assertions |
| [test/threads.test.ts](../src/core/test/threads.test.ts) | `setThreadUrl` against an unknown thread id untested | test coverage only | one `thread_not_found` case |
| [test/invites.test.ts](../src/core/test/invites.test.ts) | Cross-Weave invitee (a real participant id from another Weave) untested; facade pass-through (`core.inviteParticipant`) untested | test coverage only | add both cases |
| [test/agents.test.ts](../src/core/test/agents.test.ts) | Concurrent-join tests assert the invariant (`a \|\| b` already joined) rather than pinning the catch branch; `joinWeave`'s own per-Weave early-return lookup untested | the race cannot be forced deterministically | pin via an injected failure seam |
| [test/inbox.test.ts](../src/core/test/inbox.test.ts) | The accepted upper bound is proven only against 3 events (`limit: 1000` returns all of them), so a page genuinely truncated at the maximum is untested; default limit 100 unasserted; `helpers.js` imported twice | test coverage only | assert against a seeded page; merge the imports |
| [lobby/matching.ts](../src/core/src/lobby/matching.ts) | `requirements` cannot express `anyOf` over whole requirement sets: `models` are flat alternatives and `tools` are all required, so "either a Fable with shell, or a GPT with github" has no shape | deferred in the spec (§4) until a real request cannot be expressed with model alternatives plus required tools; the flat form covers every case seen so far, and a nested one would have to be matched, rendered and explained everywhere | add an `anyOf: Requirements[]` key matched by `some`, once something needs it |
| [lobby/requests.ts](../src/core/src/lobby/requests.ts), [lobby/invitations.ts](../src/core/src/lobby/invitations.ts) | `owner` on a profile and on a request is self-declared for a keyless participant; a keyed agent whose key names an owner is held to it (listener onboarding), and a request's owner is copied from the requester's profile | deliberate, recorded as [ADR 0001](adr/0001-lobby-owner-self-declared.md): the instance is one trusting team and the policy prevents *accidental* spending, not fraud; requiring a keeper-minted key would gate every agent's onboarding | the rest of the ADR's upgrade path: derive a request's owner from the authenticated key, require a key to register |
| [db/schema.ts](../src/core/src/db/schema.ts) | Cross-Weave invitations have no expiry: `weave_invitations` carries `redeemed_at` and `revoked_at` but no TTL; a removal from the request's Thread withdraws the removed agent's unredeemed ones, and nothing else revokes one | the request's timeout bounds the flow socially, the invitation is single-use and identity-checked, and archiving the target Weave is the containment: the same shape as a Weave secret, which also never expires | an `expires_at` defaulting to the request's, checked in `redeemInvitation` |
| [settings.ts](../src/core/src/settings.ts) | The Lobby's title is fixed at `"Lobby"` and settable only by writing `settings.lobby_title` in the database **before the first boot**: `ensureLobby` reads the column only when it *creates* the Lobby, so a later change renames nothing. It is not on the public `Settings` shape and not in the patch schema, so no keeper surface offers it | the Lobby is named once, at first boot, and the title on the Weave row is the live one; a settings key that renames nothing would be worse than one that refuses | rename the Weave itself (`weaves.title`) if renaming is ever wanted |
| [lobby/requests.ts](../src/core/src/lobby/requests.ts) | `listRequests` takes a `limit` but no cursor: it is the newest page of the chosen status, and there is no way to ask for the page behind it, so a Lobby with more closed requests than a page has history nothing can reach over the API | the surfaces that read it want the recent end — the web panel loads every *open* request separately and caps the closed history on purpose, the CLI and MCP print a board — and `createdAt` is not a cursor anyone should page on (see the row below) | page on a monotonic key: `id` with a `(created_at, id)` index, or a request `seq`, passed as `before` |
| [lobby/requests.ts](../src/core/src/lobby/requests.ts), [db/schema.ts](../src/core/src/db/schema.ts) | Every `created_at` order in the product is a **wall clock** order: `requests.created_at desc` decides which requests a page holds, and `threads.created_at asc` the order of a Weave's Thread list. Postgres stamps rows with `now()`, the transaction timestamp, and a host clock that steps backwards writes them out of order — measured on the development machine (Docker Desktop / WSL2), the database clock stepped back twice in twenty seconds, worst 1.32 s. That is what made the CLI Lobby suite flaky before `generalThreadOf`, which stopped deriving *identity* from age | nothing depends on the order for correctness any more — General is found by its flag, and a request's own state is read from its row — and on a host with a monotonic clock the display order is right; the fix is a schema change, and the visible symptom is two rows appearing swapped | order and page on a monotonic key: `events.seq` is already one per Weave, and `requests.last_event_seq` is one per request |
| [lobby/invitations.ts](../src/core/src/lobby/invitations.ts) | On an `inviteToWeave` with no request, the `weave.invited` event's `actor` in the **Lobby** log is a participant id of the **target** Weave (the inviting keeper), which a Lobby reader cannot resolve to a name | the `actor` is the principal that really acted, and rewriting it to that keeper's Lobby participant (which may not exist) would be a lie; the payload carries the target's title, which is the part a reader needs | render an unresolvable actor as the target Weave's title, or carry the inviter's display name in the payload |
| [lobby/invitations.ts](../src/core/src/lobby/invitations.ts) | The mirror of the row above, on the other side: when an invitation is redeemed, the `thread.invited` written into the **target** Weave carries `actor` and `payload.invitedBy` = `inv.createdBy`, which is a **Lobby** participant id (or `keeper:<id>`) — the accepting requester. No participant of the target has that id, so the target's own renderers fall back: the CLI prints the raw id, the channel says "unknown" and the web says "someone" | the recorded principal is the one that really issued the invitation, and it lives in the Lobby; inventing a target-side actor would be a lie, and the invitee's own name in the same event is what a reader of that Thread actually needs | carry the inviter's display name in the payload, or resolve an unresolvable `invitedBy` to the request's title |
| [lobby/invitations.ts](../src/core/src/lobby/invitations.ts) | Redeeming an invitation lands `participant.joined` on the invitation's **target Thread** rather than on the target Weave's General, unlike every other join | deliberate: whoever is waiting in that Thread should see the newcomer arrive beside the invite that asked for it, and the pair reads as one arrival | none; noted so a reader of the log is not surprised |
| [guidelines.ts](../src/core/src/guidelines.ts), [weaves.ts](../src/core/src/weaves.ts) | `getWeave` reads the single `settings` row **twice** per call, in two separate statements: `getLobbyWeaveId(db)` to decide whether this Weave is the Lobby (and so whether to blank every profile), then `getInstanceGuidelines(db)` to compose the combined text. That is on every channel metadata refresh, every `list_joined` entry, every web refresh and every per-Weave resource read | two indexed single-row reads against a table with exactly one row, and each answer has to be current; the profile-blanking check was added beside the guidelines read rather than folded into it because they are owned by different modules | read the row once and pass both columns down, or cache the settings row behind a short TTL |
| [lobby/profile.ts](../src/core/src/lobby/profile.ts), [lobby/matching.ts](../src/core/src/lobby/matching.ts), [lobby/requests.ts](../src/core/src/lobby/requests.ts) | `validateProfile` and `validateRequirements` accept a **NUL** in every string they carry — a model name, a tool, a runtime, an `owner`. Postgres `text` and `jsonb` cannot store one, so `set_capabilities` or `open_request` with a NUL in it fails with SQLSTATE `22021` (`22P05` for the jsonb column) and surfaces as `internal` (a 500) rather than as `validation`. Pre-existing, and untouched by the listeners work except that the **read** side is closed: `validateListenersQuery` rejects a NUL in `q`, in every filter string and in a text cursor key, so a hand-edited link is a 400 and never a 500. NUL is the *only* character at issue — a tab, a newline and the rest of C0 are stored by the writer and carried by Postgres on both sides (PR #20 review round 1 narrowed the read side to match) | the write paths are the same class of hole and were deliberately left out of a read-only change rather than fixed in passing; nobody has typed a NUL into a profile | reject `\u0000` in `validateProfile` and `validateRequirements` as the listeners validator already does, and say `validation` |
| [lobby/listeners.ts](../src/core/src/lobby/listeners.ts) | The two counts, the page and the four facet queries are **independent reads on pooled connections**, not one snapshot: there is no surrounding transaction, so a profile set or cleared while they run can leave a page disagreeing with its counts or its facets by one | deliberate, and the comment at the fold says so: the page is built for that tolerance (spec §5.5's "list changed — reload" hint), and a repeatable-read transaction would buy consistency for a directory nobody reads twice in the same second — at the cost of holding one pooled connection for seven statements | wrap the fold in a read-only repeatable-read transaction if a visible off-by-one is ever reported |
| [lobby/listeners.ts](../src/core/src/lobby/listeners.ts) | Every facet CTE selects `capabilities` itself into its `base`, so each of the four facet queries **materialises the whole profile document** for every matching listener before it unnests the one key it wants. The models facet does it twice (a pairs pass and an efforts pass). It is the query's expensive half, and the half that grows with profile size rather than with listener count | spec-faithful — §2.8 writes the CTEs this way, and the predicate that feeds `base` is index-backed, so the cost is bounded by `matched` rather than by the Lobby | project only the key each facet needs into its `base`, or feed the facets from one `base` CTE computed once |
| [lobby/listeners.ts](../src/core/src/lobby/listeners.ts) | `q` has **no index**: the search is `ILIKE '%…%'` over `participants.name` and `capabilities->>'owner'`, so it is a filter over the Lobby's participants rather than an index lookup | the GIN index serves the filters, `q` runs over one Weave's rows, and the alternative is a second extension | `pg_trgm` plus a GIN trigram index on `name` and on `capabilities->>'owner'`, if a search ever hurts |
| [lobby/profile.ts](../src/core/src/lobby/profile.ts), [export.ts](../src/core/src/export.ts) | **Profiles still travel over the event log.** Removing them from `getWeave` metadata removed the repeated *snapshot* and nothing else: `setCapabilities` still appends `participant.capabilities_changed` carrying the **whole** validated profile, and a web session still backfills a Weave's entire history before it reads metadata, so the Lobby's General thread grows by up to 4000 characters per profile change for ever and every loading page downloads all of them. A JSON export emits the payload verbatim | slimming the payload to `{ participantId }` is a wire-format change with readers in three packages (web `MessageList`, CLI `commands/messages.ts`, channel `format.ts`, plus the channel's wake rule and the export) and an append-only log that keeps the old shape for ever; windowing the backfill changes what every Weave page shows. Both are sub-projects of their own — the listeners spec §11 states them and deliberately does not promise either | the two options in that §11: slim the payload behind a compatibility story, or start a Lobby load from a recent seq |
| [weaves.ts](../src/core/src/weaves.ts) | **The Lobby's metadata read is still uncapped in rows.** Taking the profiles out of `getWeave` (spec §3.1) took the *documents* out; the participant **rows** are all still there — id, name, kind, role, `joinedAt`, `agentId`, with `capabilities` blanked — for every participant of the Lobby, on every page load and on every metadata refresh. A Lobby with ten thousand participants ships ten thousand rows to render a sidebar | by design, for now: a row is small and fixed-size where a profile is up to 4000 characters each, so the change that mattered is already made, and the participant list is a column of the web layout that the overhaul has not reached — the listeners directory was its first slice, not its last | page the participant list behind a count and a cursor, as the listeners directory is, when the layout overhaul reaches that column |
| [lobby/requests.ts](../src/core/src/lobby/requests.ts) | A legacy acceptance from before migration 0005 (no `due_at`) can hold a request `working` with no `request.overdue` ever sent: a request still `open` across the migration moves to `working` on its next accept, and the overdue sweep skips an acceptance with no due time | only requests open across the migration can carry one, and the requester can still cancel | give a legacy acceptance a due time at migration, or let the sweep treat a null due time as overdue after a grace period |
| [lobby/requests.ts](../src/core/src/lobby/requests.ts) | `sweepOverdue` takes the Lobby lock once per candidate acceptance, so a sweep with many overdue acceptances serialises the Lobby's writers for its length, as `sweepRequests` does | scale only: a sweep finds a handful of rows a minute | batch the candidates under one lock, or lock per request row |
| [lobby/requests.ts](../src/core/src/lobby/requests.ts) | `accept` keeps an inline copy of `recordedAuthorityHolds`, because it needs the reason for its refusal message where the helper answers only a boolean | behaviour is the same; a refactor across two call sites | let the helper return the refusal reason, and call it from `accept` |
| [lobby/profile.ts](../src/core/src/lobby/profile.ts), [lobby/matching.ts](../src/core/src/lobby/matching.ts) | The owner rule is written in three places: `validateOwner` (which claims to be the shared one), `profileSchema`, and `findAgents`' own filter check; `withKeyOwner` also repeats `validateProfile`'s shape guards before it can read `owner` | the three agree today and each is tested | one owner rule by content, called by all three, and `withKeyOwner` run on the validated profile |
| [lobby/matching.ts](../src/core/src/lobby/matching.ts) | A loose profile stored before `pollIntervalMs` was bounded may carry an out-of-range value; `matches` reads it as given | a false negative only (such an agent is not addressed by a `maxResponseMs` request), and it heals on the agent's next `set_capabilities` | clamp or ignore an out-of-range value on read |
| [lobby/matching.ts](../src/core/src/lobby/matching.ts) | A `lastSeenAt` in the future counts as live (`now - lastSeenAt` is negative), and no test pins it | the same process clock writes `lastSeenAt` and reads `now`, so only a clock step backwards makes one | treat a future stamp as now, and pin it with a test |
| [lobby/onboarding.ts](../src/core/src/lobby/onboarding.ts) | The offer-window rule and the eligibility query exist twice, in TypeScript (`offerWindowOpen`) and in the SQL of `eligibleRequests`, as `computedStatus` and `statusCondition` do | plan-mandated; both are tested | one source for the rule, as the `computedStatus` / `statusCondition` pair would also want |
| [lobby/invitations.ts](../src/core/src/lobby/invitations.ts) | `redeemInvitation` checks `revoked` before it checks the invitee, so a non-invitee holding the uuid learns that the invitation was withdrawn | the same order as the existing "already redeemed" answer; the uuid is unguessable and only its invitee is handed it | check the invitee first, then redeemed and revoked |
| [test/agents.test.ts](../src/core/test/agents.test.ts) | `setAgentOwner`'s ordering is untested: a malformed id with a bad owner is `not_found`, an unknown well-formed id with a bad owner is `validation` | test coverage only | one case each |
| [test/liveness.test.ts](../src/core/test/liveness.test.ts) | The `resolveInWeave` stamp path asserts only that `lastSeenAt` is no longer null, not the instant | test coverage only | pass a clock through and assert it |
| [test/lobby-profile.test.ts](../src/core/test/lobby-profile.test.ts) | `findAgents`' clock is not injectable, so its liveness term is tested against the real clock; the keyed-agent wording for a non-string `owner` is untested; that a refused set leaves the profile unchanged is untested | test coverage only | a `now` parameter as `onboardingFacts` has, and one case each |
| [test/lobby-requests.test.ts](../src/core/test/lobby-requests.test.ts) | The revival test does not set `completedAt` before reviving; untested: cancelling a `working` request after `expiresAt`, a string `deadlineMs`, `complete` on a legacy `open` row, and `complete` on an unknown id | test coverage only | one case each |
| [test/lobby-overdue.test.ts](../src/core/test/lobby-overdue.test.ts) | The null-`lastSeenAt` payload path is untested; the concurrent-sweep test cannot force the race (the shape of the concurrent-join row above); a cancel between the sweep's query and its lock is untested | test coverage only; the race cannot be forced deterministically | a seam between the query and the lock, as `afterMutation` is for `accept` |
| [test/thread-removal.test.ts](../src/core/test/thread-removal.test.ts) | Untested: skipping a target participant who is already removed from the work Thread, a keeper-attributed target event, and `thread.removed` in the Lobby inbox | test coverage only | one case each |
| [test/lobby-onboarding.test.ts](../src/core/test/lobby-onboarding.test.ts) | The "my own request" case proves nothing: the eligibility snapshot already leaves the requester out, so `ne(requesterId)` is never the reason (the test says so in a comment). Missing: a `working` request inside and outside its offer window, and an `open` request the agent is not eligible for | test coverage only | a request whose snapshot names the requester, made by hand, and one case each |

## server

| Area | Issue | Why deferred | Suggested fix |
| --- | --- | --- | --- |
| [package.json](../src/server/package.json) | `hono-rate-limiter@0.5.4` is a declared runtime dependency but imported nowhere; no rate limiting is wired up | dependency hygiene, no behaviour today | wire it on the public routes, or drop the dependency |
| [auth.ts](../src/server/src/auth.ts) | The bearer middleware builds `new URL(c.req.url)` per request only to test the path | negligible cost | use `c.req.path` |
| [routes/weaves.ts](../src/server/src/routes/weaves.ts), [mcp/backend.ts](../src/server/src/mcp/backend.ts) | `POST /api/weaves` (and `CoreToolBackend.createWeave`) resolve a present credential strictly: a stale token gets 401 on an open instance instead of creating anonymously | matches the deliberate strict-join ruling (ChatGPT F5) | document it, or swallow `invalid_token` while creation is open |
| [mcp/index.ts](../src/server/src/mcp/index.ts) | Remote MCP `initialize` reads the database on **every** new session (`core.getInstanceGuidelines()`), so a handshake that used to be pure now fails with a 500 during a database outage | deliberate: `McpServer` fixes `instructions` at construction, and reading per session is what makes a keeper's edit reach the next connection without a restart | give it the deadline-and-fallback the channel has (`fetchInstanceGuidelines`, 2 s, mechanics text alone), or cache the text with a short TTL |
| [app.ts](../src/server/src/app.ts) | Route order (`/api/weaves/:secret/lookup` before `/api/weaves/:id`) is not pinned by a test | works today, fragile to reordering | a regression test on both paths |
| [main.ts](../src/server/src/main.ts) | **The first boot prints the Lobby's link — `/w/<43-character secret>` — unredacted**, on the one boot that creates the Lobby. It is deliberate and the README documents it: nobody created that Lobby, so nobody was handed its secret, and the printed link is how a first operator finds it. The cost is that the secret is in whatever reads that boot's log: a `docker … logs` run over SSH puts it in the controlling session's transcript, where a credential must never appear | by design, and the exposure is closed at the **reader** rather than at the writer for this slice: every log the live-instance work reads goes through a `redact_logs` filter, and the first deployment's done-check asserts a shape on the server instead of printing the log. Gating the line itself alters an interface the README documents and a first-run path nothing else has exercised, so it is a later slice's change rather than this one's | gate that one line behind an explicit flag (`LOOM_PRINT_LOBBY_LINK=1`, say) and have the unflagged boot print the Lobby's **id** alone; the idea, with this decision attached, is in [superpowers/specs/v2-notes.md](superpowers/specs/v2-notes.md) |
| [test/](../src/server/test) | No direct tests for bearer parsing / `requireActor` / error mapping beyond the route tests | test coverage only | unit-test the middleware |
| [test/ws.test.ts](../src/server/test/ws.test.ts) | Live-phase tests use fixed 150 ms sleeps to prove the absence of duplicates | proving a negative needs a wait or a seam | inject a scheduler seam |
| [test/mcp.test.ts](../src/server/test/mcp.test.ts) | `set_thread_url`, `invite_participant`, `create_thread(url)` and `keeper_agents_*` are not exercised at the MCP level (only against the fake backend in mcp-tools) | wiring is thin and typed | one round-trip per tool |
| [routes/weaves.ts](../src/server/src/routes/weaves.ts) | `POST /api/weaves/:id/invitations` answers `{ invitationId, seq }`, a superset of the `{ invitationId }` the spec's REST table names | the `seq` is what core already returns and what a caller needs to place the `weave.invited` in the Lobby log; dropping it in the adapter would hide it for no reason | none — the spec table is the one that was narrowed (§3 corrected) |
| [app.ts](../src/server/src/app.ts) | A throwing `sweepRequests` skips `sweepOverdue` for that tick: `sweepNow` runs the two passes in sequence and the interval logs the first failure | the next tick, a minute later, runs both again; the overdue pass is at most a tick late | run each pass under its own `catch`, logging each failure |
| [routes/requests.ts](../src/server/src/routes/requests.ts) | `POST /api/requests/:id/complete` with no body is a 400 `validation` ("Body is not valid JSON"): a client must send `{}` at least | the route parses the body as every other route does; the client always sends one, and the server README says so | treat an empty body as `{}` on this route |
| [mcp/index.ts](../src/server/src/mcp/index.ts) | `oninitialized` may fire more than once per session for a client that sends the initialized notification twice, which writes the `mcp: session initialized` line twice | a duplicated log line, harmless | log once per session, with a flag on the server |
| [test/](../src/server/test) | The new routes' 400 (bad body), 401 (no credential) and unknown-Thread paths are untested (`POST /api/requests/:id/complete`, `POST /api/threads/:id/removals`, `PUT /api/admin/agents/:id/owner`) | test coverage only; the error mapping is shared and tested on the older routes | one case per path |
| [test/mcp.test.ts](../src/server/test/mcp.test.ts), [origin.ts](../src/server/src/origin.ts) | The client version's cleaning, an empty `X-Forwarded-Proto` and an IPv6 `Host` are untested (the 100-character cut is tested) | test coverage only | one case each |

## mcp-tools

| Area | Issue | Why deferred | Suggested fix |
| --- | --- | --- | --- |
| [tools.ts](../src/mcp-tools/src/tools.ts) | 20 × `toToolResult(Promise.resolve().then(() => …))` boilerplate | readability only | a small `call()` helper |
| [tools.ts](../src/mcp-tools/src/tools.ts) | The keeper-token hint does not mention that `credential` is optional on an agent connection | cosmetic | reuse the agent-aware hint |
| [test/tools.test.ts](../src/mcp-tools/test/tools.test.ts) | The `credential is required on this connection` branch is untested (an empty-string `credential` now reaches it, since the schemas carry types only); `join_weave`'s credential forwarding is not asserted by the fake; no table-driven test over all 38 tool→backend mappings; `result.ts` edge cases (non-`Error` throw, string payload) untested | test coverage only | call `registerLoomTools` directly for the branch; add the table |
| [test/tools.test.ts](../src/mcp-tools/test/tools.test.ts) | `get_started`'s second refusal branch (a credential present, but a backend without `onboardingFacts`) is untested; `join_weave`'s `next` is tested on the invitation path only; `withNext` passing a non-object result through unchanged is untested | test coverage only | one case each, against a stub backend without `onboardingFacts` |

## client

| Area | Issue | Why deferred | Suggested fix |
| --- | --- | --- | --- |
| [client.ts](../src/client/src/client.ts) | `weaveId` / `threadId` path segments are interpolated without `encodeURIComponent` (secrets are encoded) | ids are server-generated uuids | encode for house style |
| [http.ts](../src/client/src/http.ts) | `redirect: "error"` means an `http://` base URL behind a proxy that answers 301/308 with the https location now fails as `network` ("Server redirected the request") instead of being followed silently | deliberate: following a redirect escapes the one-time https-only URL policy. No documented flow redirects — the CLI, the channel and the web app all address the server directly | none; configure the client with the https URL the proxy terminates at |
| [test/client.test.ts](../src/client/test/client.test.ts) | `thread.url_changed` never asserted; the `listAgents` assertion assumes exactly one agent | test coverage only | assert the event; assert by `find` |
| [test/client.test.ts](../src/client/test/client.test.ts) | The round trip checks neither `seq` nor `targetRemoved` of `removeParticipant`, nor an `acceptRequest` without `deadlineMs` (the server's `validation`) | test coverage only; the rules are core's and tested there | assert both fields, and one refused accept |

## cli

| Area | Issue | Why deferred | Suggested fix |
| --- | --- | --- | --- |
| [commands/invite.ts](../src/cli/src/commands/invite.ts) | `--since` / `--limit` integer parsers are duplicated inline instead of reusing the parser in [commands/messages.ts](../src/cli/src/commands/messages.ts) | duplication only | export and reuse `intParser` |
| [cli.ts](../src/cli/src/cli.ts) | A valueless `--url` reports "unknown option '--url'" rather than "option requires a value"; `takeBaseUrl` stops at the first token matching a command name (an option *value* equal to a command name ends the scan) | both are odd-shaped inputs; the global `--url` must stay out of commander's reach | parse the head with a tiny explicit scanner |
| [context.ts](../src/cli/src/context.ts) | `LOOM_AGENT_KEY` silently wins over a stored participant token for the Weave | plan choice, flagged for the owner | warn on stderr, or add an explicit `--as` |
| [config.ts](../src/cli/src/config.ts) | `ConfigStore.update` is a sequence of separate operations, none of them atomic against the others. The dead-owner check and the `rmSync` that reclaims the lock are two operations, so two reclaimers can observe the same dead lock, and a reclaimer delayed between its check and its unlink can remove the *live* lock its successor has already taken — admitting a third writer. The lease re-check before `save()` catches a writer that lost the lock that way everywhere except inside its own check/save window, where a stale snapshot can still be written over a successor's | mutual exclusion here rests on windows between operations rather than on an atomic primitive; the exposure is narrow, not closed, and a duration is not a synchronization guarantee. It needs a dead owner process *and* a writer descheduled between two adjacent statements, which single-user CLI use does not produce | compare-and-swap on the config file (write a temp and link it against an expected version), as the channel's `ChannelState` does |
| [test/](../src/cli/test) | The `--url=<value>` form is untested (only `--url <value>`) | test coverage only | one case |
| [commands/lobby.ts](../src/cli/src/commands/lobby.ts) | An instance keeper alone cannot read the register: `lobbyContext` requires a stored Lobby token (or `LOOM_AGENT_KEY`), so `LOOM_KEEPER_TOKEN=<token> loom lobby` fails `no_lobby_token` unless that config has already run `loom lobby join`. The keeper credential is used only to *learn* the Lobby's secret, never to authorise the read — so the one command that prints the Lobby's web link needs a participant identity it does not otherwise need. Found in the Lobby smoke test of 2026-09-17 | the two credentials answer different questions (where the Lobby is vs. who is asking), and a keeper is not automatically a participant; `GET /api/lobby` with the keeper bearer returns the secret, so nothing is unreachable. **The live-instance runbook is a caller that has to do the work-around**: the first deployment runs a `loom lobby join` as the keeper before it can read the Lobby link at all (the live-instance spec, section 9 step 10) | let the keeper credential — or the Lobby secret it just read — stand in as the credential for the read, so `loom lobby` works from a keeper shell with no join |
| [commands/lobby.ts](../src/cli/src/commands/lobby.ts) | `loom lobby` merges `findAgents({})` into `getWeave`'s participants to keep its output unchanged, and `findAgents` takes no limit and no cursor: it loads **every** profile in the Lobby into memory and matches them there. The command's `getWeave` half is unbounded for the same reason — it returns every participant — so the pair is one unbounded read beside another, on the one command a human runs to see who is standing there | it is what `find_agents` already does for every agent on the instance, the CLI prints one line per participant anyway, and the paged alternative (`listListeners`) is deliberately not wired to a new CLI command in this change (spec §11) | page the command on `listListeners` once a Lobby is big enough that a full listing is not what a human wanted |
| [commands/request.ts](../src/cli/src/commands/request.ts) | `request open` and `invite-weave` name their **target** Weave through the *global* `--weave`, which is optional and falls back to `lastWeave`. So the target of a request is whichever Weave you last created or joined unless you say otherwise, and it is a global option rather than one on the command that uses it | there is only one `--weave` and every other command means the same thing by it; `lobby join` deliberately does not move `lastWeave`, so the Lobby never becomes the silent default — and core refuses a request that targets the Lobby anyway (`A request cannot target the Lobby`) | require `--weave` on these two commands, or give them a `--target-weave` of their own |
| [commands/invite.ts](../src/cli/src/commands/invite.ts) | `loom remove` uses the token stored for the current Weave, so on a request's Thread (the reaction to `request.overdue`) the requester must pass `--weave <lobbyId>` or set `LOOM_AGENT_KEY`, where every `loom request ...` command finds the Lobby by itself through `lobbyContext` (whole-branch review, M4). Its help also names a keeper, but the CLI never sends the instance keeper token | documented in the command's help, the CLI README and smoke test 7; a fallback is a CLI behaviour change | let `remove` fall back to the Lobby context when the Thread is a request's, and say in the help which keeper it means |
| [test/](../src/cli/test) | The remover's name in the `thread.removed` line is unpinned and its Keeper branch untested; untested: a malformed `--deadline`, an out-of-range deadline, `admin agents set-owner`'s human output and its ambiguous-name refusal, and the `targetRemoved` suffix of `remove` | test coverage only | one case each |

## claude-channel

| Area | Issue | Why deferred | Suggested fix |
| --- | --- | --- | --- |
| [streams.ts](../src/claude-channel/src/streams.ts) | `applyToNames` folds a foreign `thread.created` into the names cache but does not register it in `threadToWeave`/`threadIds`, so `credential="stored"` resolution by that thread id waits for the next successful refresh | only reachable when the refresh right after the event fails | register the thread there, guarded like `noteThread` |
| [streams.ts](../src/claude-channel/src/streams.ts) | `preambleDone` is process memory, not persisted session state, so a `--resume` (a new channel process for the same `CLAUDE_CODE_SESSION_ID`) re-sends the guidelines preamble. It is also keyed by weaveId alone, so rejoining under a *new* identity without calling `leave_weave` first (the only thing that clears it) keeps the flag and the new participant never sees the rules. Conversely, in `wake: "mentions"` a Weave that never mentions you produces no first *woken* event, so that session never receives its preamble at all | both are the safe side: re-sending current rules costs a few hundred tokens, and persisting the flag would let a resumed session act on rules it was never shown. The mentions-only gap is inherent to folding the preamble into a woken event — the text is still reachable through `list_joined` and the per-Weave resource | persist the flag per session alongside the delivery cursor if the repeat becomes noisy; for the mentions-only case, deliver the preamble on the first *delivered* event instead of the first woken one |
| [format.ts](../src/claude-channel/src/format.ts) | `who()` falls back to `{ name: "unknown", kind: "unknown" }` for any actor that is not a participant of the Weave, so the channel tag on a sweeper-issued `request.closed` (actor `system`) reads `from="unknown" from_kind="unknown"`, as does the target-side `thread.invited` whose actor is a foreign Lobby participant id (the two `lobby/invitations.ts` rows above). Seen live in the Lobby smoke test of 2026-09-17 | cosmetic: the body of the event says what happened, and "unknown" is at least not a wrong name | render the literal actor `system` as `from="system" from_kind="system"`, and give the foreign-participant case the fallback the core rows suggest |
| [format.ts](../src/claude-channel/src/format.ts) | The `request.closed` body reads `Request "<title>" expired: accepted ChatGPT`, which the requester agent in the 2026-09-17 smoke test read as *the acceptance* having expired rather than the request closing with that helper still in place | cosmetic wording; the `accepted` list and the request's own state are correct, and `get_request(<id>)` says the same thing unambiguously | say what the close means for the helper, e.g. `Request "<title>" expired with 1 of 2 filled (ChatGPT stays invited)` |
| [backend.ts](../src/claude-channel/src/backend.ts), [state.ts](../src/claude-channel/src/state.ts) | Every join stores the new Weave with `lastSeq: 0` (`join_lobby` included) and `upsertWeave` seeds this session's cursor from it, so in `wake: "all"` a fresh session is woken by that Weave's history from seq 1 — in the 2026-09-17 smoke test, a session joining the Lobby was woken by "General created" and "paw joined" | pre-existing join behaviour and the safe side generally (a new participant sees what it walked into); only the Lobby makes it loud, because every agent's join and profile change lives there | start a join at the Weave's current `lastSeq` rather than 0, or exempt the Lobby's membership events from the `wake: "all"` fallback the way the addressed-only Lobby rules already exempt requests |
| [backend.ts](../src/claude-channel/src/backend.ts) | A nameless `join_weave` over the channel plugin returns `validation: name is required` instead of reusing the stored participant name (the channel is not an agent connection) | the agent-key path covers remote clients; the channel always passes a name today | fall back to the stored `participantName` |
| [state.ts](../src/claude-channel/src/state.ts) | `ChannelState.setWake` is `@deprecated` and used only by its own test; `JoinedWeave.wake` is read only as a legacy fallback in `prefs()` | vestigial API kept for a migration window | remove both once no stored config carries a machine-wide wake |
| [state.ts](../src/claude-channel/src/state.ts) | The temp-file sweep keys on the writer pid in the file name, so a `.tmp` whose dead writer's pid has since been reused by an unrelated live process is never swept and leaks | leaking one small file is the safe side of the trade: the alternative deletes a live writer's file and turns a join into a lost participant token | record the writer's process start time alongside the pid, or fall back to the age cutoff once the file is far older than any plausible write |
| [test/](../src/claude-channel/test) | Backend pass-throughs (`set_thread_url`, `invite_participant`, `inbox` at the channel tool level) untested; the e2e drives them through core instead | wiring is thin | one tool call each |
| [test/channel.test.ts](../src/claude-channel/test/channel.test.ts) | The second `set_wake({invites:false})` return is unasserted (wake preservation on a partial patch); the suppression assertion is a bare boolean with a weak failure message; `dirB` is a pointless alias of `stateDir`; two live channel processes share one state dir | test hygiene only | assert the patch result; name the suppressed event in the message |
| [backend.ts](../src/claude-channel/src/backend.ts) | The MCP resource reads (`getInstanceGuidelines`, `getGuidelines` behind `loom://guidelines` and `loom://weaves/{id}/guidelines`) carry no deadline, unlike the startup instance fetch (2 s) and `list_joined` (2 s per Weave): a server that accepts the request and never answers parks the read for as long as the connection lives | a resource read is one explicit call the agent chose to make, not a fan-out, and it fails visibly rather than silently degrading a listing | give both the `AbortSignal.timeout` treatment `list_joined` now uses |
| [test/](../src/claude-channel/test) | Two guidelines paths are untested: a notify rejection before `preambleDone` is set replays the preamble on the restarted stream (the flag is only added after a successful delivery), and a `weave.guidelines_changed` raised by this session's *own* actor refreshes the cached text without waking anyone | both need a seam to force the failure or a second identity in the e2e; the behaviour is argued in the comments at the call sites | inject a failing notify; post the change as the session's own participant and assert `entry.guidelines` moved with no notification |
| [test/](../src/claude-channel/test) | The Keeper-attribution and string-`lastSeenAt` formatting paths of the new events are untested; the stored-removal test polls for up to 5 s | test coverage and speed only | one case each; drive the stored-removal case from an event rather than a poll |

## web

| Area | Issue | Why deferred | Suggested fix |
| --- | --- | --- | --- |
| [components/ThreadTools.tsx](../src/web/src/components/ThreadTools.tsx) | The URL field seeds from the thread once per mount: saving after another editor changed it silently reverts their value | single-editor use in practice | re-seed on change, or compare-and-set on save |
| [components/GuidelinesPanel.tsx](../src/web/src/components/GuidelinesPanel.tsx) | Saving text equal to what is stored is only *prevented* (Save is disabled while `unchanged`) — there is no "Guidelines unchanged" feedback, so core's `seq: null` case has no UI counterpart the way `loom guidelines set` prints one | the disabled button makes the state visible before the click; a message with nothing to report after it is noise | surface `seq === null` as a transient line if the panel ever allows submitting an unchanged draft |
| [components/GuidelinesPanel.tsx](../src/web/src/components/GuidelinesPanel.tsx) | No concurrent-edit detection: the draft seeds from the Weave once when Edit is clicked, so another keeper's change arriving while the form is open is overwritten on Save without a word — the same shape as the ThreadTools URL row above | single-keeper use in practice, and the panel already re-derives *authority* on every render, so the loss is confined to text | compare-and-set against the `lastSeq` the draft was seeded at, or re-seed and warn when `state.weave.guidelines` changes under an open form |
| [session.ts](../src/web/src/session.ts) | `deriveInvites` rescans the whole event log on every event | logs are small in practice | maintain the map incrementally |
| [components/RequestsPanel.tsx](../src/web/src/components/RequestsPanel.tsx) | The Offer form disappears once this browser's participant has an offer on the request, so an offer cannot be revised: there is no way to change the model or the note from the web UI (core is idempotent and would return the first offer anyway) | an offer is "I can take this now", not a document; a form that silently no-ops would be worse than no form | show the submitted offer read-only in its place, and add a revision path in core first if one is ever wanted |
| [components/InviteBanner.tsx](../src/web/src/components/InviteBanner.tsx) | The banner is not an `aria-live` region, and its text is not clickable to open the Thread (only the × dismisses) | accessibility polish | wrap in `aria-live="polite"`, make the line a button |
| [test/](../src/web/test) | `shortUrl`'s truncation branch and `MessageList`'s "no longer links to an artefact" (`url: null`) branch untested | test coverage | add both branches |
| [weaves-store.ts](../src/web/src/weaves-store.ts) | A legacy `loom:<secret>` key whose value is not JSON, is JSON that is not an object (`null`, a string, an array), or carries no string `token` is silently skipped by `storedWeaves` and `migrateLegacyOne`, so it never becomes a row, never migrates, and **Forget** cannot reach it either (`forgetWeave` needs a `weaveId`): it stays in `localStorage` for good. The old code pruned an unparseable key on read | deleting on a failed parse is how a browser loses an identity to a transient bug in the reader, which is the worse failure of the two; the leak is a few hundred bytes | offer a "storage this browser cannot read" row with its own Forget, keyed by the raw key rather than by a Weave id |
| [components/main/MyWeaves.tsx](../src/web/src/components/main/MyWeaves.tsx) | Every write bumps the change signal per row, and each bump re-runs `storedWeaves(storage)`, which re-reads and re-parses **every** stored entry. Draining 300 rows is ~300 full re-parses | the bump-per-write is what keeps the list honest with storage (spec §4.2), and at the tens of rows a real browser holds the cost is invisible; it is quadratic only at hundreds | debounce the bumps into one microtask, or version the parse and re-read only the key that changed |
| [components/main/LobbySummary.tsx](../src/web/src/components/main/LobbySummary.tsx) | "3 open requests" is counted by fetching a page of up to 1000 open requests and taking `.length`, because `listRequests` has no count and no cursor (the [`lobby/requests.ts`](../src/core/src/lobby/requests.ts) row above). A Lobby with more than 1000 open requests would undercount silently | one bounded request, made only for a browser that already holds a Lobby token, and the alternative is a new endpoint | a count endpoint, or `X-Total-Count` on `listRequests` — it belongs with the cursor work |
| [components/main/InstanceGuidelines.tsx](../src/web/src/components/main/InstanceGuidelines.tsx) | Collapsing past 12 lines slices raw source lines, so guidelines whose fenced code block straddles the cut render with an unterminated fence until "Show all" is pressed | collapsing by source line is what makes the unit the line the keeper wrote, and slicing before the renderer keeps its escaping intact; instance guidelines are prose in practice | extend the cut to the end of an open fence, or collapse with CSS `max-height` over the fully rendered text |
| [name.ts](../src/web/src/name.ts) | `isValidName` tests `NAME_RE` directly while core's [`validateName`](../src/core/src/names.ts) **trims first**, so ` dana ` is refused by the browser and accepted by the server — the client is marginally stricter than the rule it mirrors | the spec states the regex, the forms disable Join rather than failing a request, and being stricter cannot produce a rejected write | trim in `isValidName`, or trim the field value before validating and submitting |
| [test/one-storage-instance.test.ts](../src/web/test/one-storage-instance.test.ts) | The §2.4a guard excludes the storage module by **basename** (`storage.ts`) and matches the literal `browserStorage(`, so a second file named `storage.ts` in any subdirectory is exempt and an aliased import (`import { browserStorage as mk }`) is not seen at all | it catches the mistake it exists for — a component or hook constructing its own store — and a test that parses imports is a lot of machinery for an invariant one reviewer glance also covers | compare resolved paths instead of basenames, and match on the import specifier rather than the call site |
| [components/PersistenceBar.tsx](../src/web/src/components/PersistenceBar.tsx), [styles.css](../src/web/src/styles.css) | `.persistence-bar` was styled for the main page's 680px column; on a Weave page it is rendered above `.layout` and spans edge to edge with a gap above the header. The DOM tests assert only that exactly one bar is present. It was **seen** in manual smoke test 5 of 2026-09-19 — as was the in-place title button in My Weaves — and no defect was raised, but neither was looked at deliberately, so there is no sign-off on either. The way back to `/` added since joins them: the **Loom** wordmark (`.home-link` in `Header.tsx`) and the in-place "Go to the main page" button (`.home-back` in `HomeLink.tsx`), both asserted by the DOM tests and neither ever seen in a browser | cosmetic, and the first two only on a browser that is already refusing to store anything | look at all of them on purpose in the next run of smoke test 5 — step 7 now walks the wordmark's round trip — and give the bar a Weave-page variant if it needs one |
| [session.ts](../src/web/src/session.ts) | Opening someone's `/w/<secret>` link now writes a `loom:weave:<id>` entry — the secret, the title and `lastOpenedAt` — **before any join**, so merely reading a Weave leaves it listed in My Weaves on that browser. Deliberate (spec §10.9): it is what makes the secret fallback and the read-only row state coherent | the trace is local only, nothing is sent, and the row can be Forgotten; the alternative is a browser that can read a Weave and cannot find it again | nothing planned — if it ever needs an opt-out it is a setting on the page, not a rule |
| [weaves-store.ts](../src/web/src/weaves-store.ts) | An entry is **never** deleted automatically (spec §10.10): a 401 clears the identity and keeps the row, a 404 greys it, and only **Forget** removes one. A browser therefore accumulates rows for Weaves it can no longer reach | nothing the user was given should vanish without them saying so, and a cached title is what lets them ask a keeper for a link | nothing planned; an "archive/hide" action is the polite middle if the list ever gets noisy |
| [storage.ts](../src/web/src/storage.ts) | A write that did not reach `localStorage` survives as a pending override for **the life of the page only** — a reload starts from `localStorage` alone — and overrides are never shared between tabs, so a second tab opened while storage is blocked sees none of the first tab's credentials | the override exists to stop a page contradicting itself, not to be durable storage; retrying in the background or syncing across tabs would both be inventing persistence a browser has just refused | none; the bar of §6 tells the human what actually to do (copy the link, or allow site data) |
| [components/listeners/ListenersPage.tsx](../src/web/src/components/listeners/ListenersPage.tsx), [components/ListenersLink.tsx](../src/web/src/components/ListenersLink.tsx), [styles.css](../src/web/src/styles.css) | **Nothing covers how the directory looks.** Narrowed on 2026-09-20: the *structural* half of this row is settled — the directory is now a **view inside the Lobby's layout** (`feat/lobby-listeners-view`), so the header, the sidebar and the composer stay on screen, and the page's own wordmark and **Back to the Lobby** are gone with the oddity of a wordmark jammed against a headline. What is **not** settled is the **styling**, and none of it is signed off: the card grid, the facet chips (including the selected-and-zero chip and the **nested effort row**, which pushes the next model chip far to the right — finding 4 of the 2026-09-20 run, deliberately not specified), the counts line, the `partial` notice, the sidebar's **Listeners (N)** line in its two *states* (it is always a button now, plain or `aria-current`), and the two new class hooks the view introduced, `.listeners-view` (with its `<h2>Listeners</h2>`) and `.composer-slot`. `.composer-slot` has **no** rule at all; `.listeners-view` carries exactly one, added on the owner's word of 2026-09-20 — `flex: 1; min-height: 0; overflow-y: auto`, which is what makes the open directory scroll inside the layout instead of scrolling the header and the sidebar off the screen. That rule is layout, not appearance: how the view **looks** is still entirely open | appearance is not testable in happy-dom, and the visual design is **the owner's** to make, not the implementation's | it belongs to the owner's **separate design session** and to nothing else — the listeners-view spec says so in as many words, and the plan's Global Constraints forbade any task on that branch from prescribing styling — the one `.listeners-view` rule above was added afterwards, by the owner's explicit decision on 2026-09-20, because it is behaviour the spec's own success scenario promises. One binding constraint the design session must honour: **no CSS rule may give `.composer-slot` a `display`**, because that would defeat the UA's `[hidden]` and put a hidden composer back in the tab order. **Manual smoke test 6** in [TESTING.md](TESTING.md) is the place the look is judged; the 2026-09-20 run's notes are in [v2-notes.md](superpowers/specs/v2-notes.md) |
| [components/listeners/ListenersPage.tsx](../src/web/src/components/listeners/ListenersPage.tsx) | The directory has **no live updates**, by design: a page holds the answer it asked for, and a profile declared, changed or cleared while it is open changes nothing on screen. The only signal is the "list changed" hint — the page re-reads `total` and offers **Reload the list** when it moves — which notices a listener arriving or leaving and not a profile being edited | a directory that reshuffles under a reader's cursor while they are reading it is worse than a stale one, and a stream plus a "reshuffle politely" rule is its own design (spec §11) | a stream and an explicit refresh rule, if the hint ever proves too passive |
| [components/listeners/listeners-query.ts](../src/web/src/components/listeners/listeners-query.ts), [components/listeners/ListenersPage.tsx](../src/web/src/components/listeners/ListenersPage.tsx) | The "part of this link was not understood" notice **latches for the life of the page**. It describes the link the page was opened with, which the reader can still see in the address bar — until the first control change, after which `replaceState` has rewritten the query string and the line arguably describes nothing | it is one line of muted text, and clearing it on a control change would mean the notice disappears from a page whose *rows* still came from the misread link | clear `partial` on the first control change, if the stale line is ever confusing |
| [components/listeners/listeners-query.ts](../src/web/src/components/listeners/listeners-query.ts) | The "nothing is dropped silently" rule covers the four keys this page's encoding *has* (`q`, `filter`, `sort`, `dir`) and every entry inside them. A hand-typed `?limit=100` or `?cursor=…` is ignored **without** the notice, because neither is part of the encoding: a link reproduces a view, not a page position | deliberate and stated at the type (`ListenersView`), and reporting every unknown query parameter would make an analytics tag or a `?utm_source=` look like a broken link | report a supplied `limit`/`cursor` as `partial` if anyone ever hand-writes one and is surprised |
| [components/listeners/FacetChips.tsx](../src/web/src/components/listeners/FacetChips.tsx), [components/listeners/listeners-query.ts](../src/web/src/components/listeners/listeners-query.ts) | A hand-written link may list the **same model twice at different efforts** (`[{"model":"m","effort":"high"},{"model":"m","effort":"low"}]`). Core answers it correctly — model alternatives are any-of, so the union of both efforts matches — but the chips render only the **first** of the two as the selected effort, and clicking the model chip off removes both alternatives at once. The page's own controls cannot produce the shape: `toggleEffort` rewrites the one alternative it finds | URL-only and cosmetic; the rows on screen are the right rows, and the only wrong thing is which effort chip looks pressed | key the effort selection by the pair rather than by the model, or fold duplicate models into one alternative per model when the link is read |
| [components/listeners/ListenersPage.tsx](../src/web/src/components/listeners/ListenersPage.tsx) | Re-measured 2026-09-20 on `feat/lobby-listeners-view`: the file is **335 lines**, of which `ListenersPage` itself is `:56-308`, still holding the whole query lifecycle — ten `useState`/`useRef` cells including the generation ref, the liveness ref and the debounce timer, the four status branches, Show more with its own error and appending flags, the `replaceState` call, and now the two-call credential rule (`session.listListeners`, then `session.reportCredentialFailure` behind its own guard) and CR5's `atDefaults` — beside the markup that renders it. It did **not** grow by becoming a view: it shed the props, the resolver and the join form that `ListenersRoute` used to hand it, and gained the counts line and Clear filters, which is roughly a wash | it is one coherent state machine and splitting it while the rules were still being fixed would have moved the bug surface rather than shrunk it; the rules themselves are tested | extract a `useListenersQuery` hook holding the generation, the debounce and the four cells, leaving the component as markup over its return value |
| [session.ts](../src/web/src/session.ts) | The Lobby page now makes **two extra requests per metadata refresh** — `listListeners({ limit: 0, facets: false })` for the count and `getMyLobbyParticipant()` for this browser's own profile — where both used to fall out of the one `getWeave` snapshot. A refresh is scheduled by every event in the Lobby, so a busy Lobby costs three requests where it cost one | it is the trade the change is for: the snapshot carried *every* profile on *every* refresh, and two bounded reads are cheaper than that at any Lobby size worth having a directory for. Both are non-fatal, so neither can fail the refresh | coalesce the count into the refresh's own `Promise.all` only if it is ever acceptable for its failure to cost the whole refresh — it is not, which is why it is caught on its own |
| [components/main/JoinLobbyForm.tsx](../src/web/src/components/main/JoinLobbyForm.tsx) | A Lobby identity lost with the browser's storage cannot be reclaimed: `name_taken` is answered with an explanation and a client-side suffix suggestion (`dana` → `dana-2`), and the original name stays held by a participant nobody can act as | the only proof that the claimant is the earlier participant is the token that was lost; anything else is a keeper doing it by hand | a keeper-side "release this participant's name" or rename, if it ever becomes a real annoyance |
| [session.ts](../src/web/src/session.ts) | A `thread.removed` in a target Weave that carries a `requestId` triggers a request refresh in a session that is not the Lobby's | harmless: that session's requests map is empty, so the refresh changes nothing | apply the request arm only in the Lobby's session |
| [requests-state.ts](../src/web/src/requests-state.ts) | After a `request.accepted` event the acceptances are in event order, which can differ from the server's order until the next snapshot replaces them | cosmetic, and short-lived | sort acceptances as the server does when the event is applied |
| [test/](../src/web/test) | The revive path (an acceptance removed and accepted again) and a request panel control disappearing once its condition ends are untested (Task 16 review) | test coverage only | one case each |

## tooling/docs

| Area | Issue | Why deferred | Suggested fix |
| --- | --- | --- | --- |
| [pnpm-workspace.yaml](../pnpm-workspace.yaml) | `onlyBuiltDependencies` approves `cpu-features` / `ssh2` (pulled in by `@testcontainers`): install prints native-build noise, and fails those builds, on Windows without a VS toolchain | optional native deps; tests pass regardless | note it in [CONTRIBUTING.md](../CONTRIBUTING.md), or drop them from the allowlist |
| [src/server/package.json](../src/server/package.json) | The dev server script is `tsx watch src/main.ts`; started from a **non-interactive** launcher (the shape an agent runs commands in) it prints nothing at all and never finishes booting. Hit while setting up the Lobby smoke test of 2026-09-17; plain `npx tsx src/main.ts` in the same place boots and logs normally | a developer's own terminal is the supported way to run it, and the workaround is one command | document the workaround beside the run instructions, or give the package a watch-less `dev:once` script |

## Product-level gaps

Not defects — features consciously out of scope. They are listed in
[superpowers/specs/v2-notes.md](superpowers/specs/v2-notes.md) and should not be reported as
findings either:

- **Deferred from v1**: pushing into a remote agent's platform (no inbound path exists today),
  `claude/channel/permission` relay, event-sourced projections/replay, per-Thread keepers,
  GitHub/PR integration, publishing the channel plugin through a marketplace.
- **No listener runtime except the Claude Code channel.** The Lobby delivers `request.opened` to
  eligible participants and `weave.invited` to invitees, but something has to be awake to receive
  them. The channel plugin is that for Claude Code. For ChatGPT, `get_started` teaches a
  scheduled-task poll, and `lastSeenAt` shows whether it is running; Loom cannot start it, keep it
  alive or restart it, and a stopped poll is found out through `lastSeenAt` and `request.overdue`,
  not prevented. A long-lived "Loom agent runner" holding the stream with an agent key is its own
  sub-project (the Lobby spec §9), not a defect.
- **The web UI does not scale to a busy instance.** The listener column is now done — the sidebar's
  stack of profile cards is one **Listeners (N)** line into a searchable, filterable, paged directory
  — but the rest of the Weave page is untouched: Threads, Guidelines and Requests still stack in one
  column, and there is no Weave switcher. It is its own sub-project ("Web client layout for a busy
  instance" in v2-notes, of which the listeners page was the first slice) and the remainder meets the
  cursor-less `listRequests` row above — paging requests needs a cursor the API lacks.
- **Dogfood findings**: org policy silently blocking channel delivery (needs a README note), keeper
  tools always advertised (10 of 38) even on an agent connection where they can never apply,
  Cloudflare quick-tunnel flags needed on some networks, and `run.cmd`/`run.ps1` dying with a raw
  `EADDRINUSE` stack trace when port 3000 is held.

See that file for the full text and context.

## How to close an item

Fix it and delete its row in the same PR — the register must never describe something that is
already fixed. If the fix turns out to be bigger than the row suggests, either leave the row alone
or rewrite it to say what was learned; do not mark rows as done in place.
